const form = document.querySelector('form')
const parkSelect = document.getElementById('park-select')
const searchBtn = document.getElementById('search')





searchBtn.addEventListener('click', displayParks)